{
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run dev",
    "build": "npm run build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "firebase": "^9.18.0"
  }
}
